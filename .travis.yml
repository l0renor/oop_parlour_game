language: java
addons:
  sonarcloud:
    organization: "l0renor-github" # the key of the org you chose at step #3
script:
  # other script steps might be done before running the actual analysis
  - mvn clean package sonar:sonar
